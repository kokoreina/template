apiVersion: v1
kind: Pod
metadata:
  name: good-pod
  namespace: demo
spec:
  securityContext:
    runAsUser: 1000
  containers:
  - name: nginx
    image: nginx:1.27
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
