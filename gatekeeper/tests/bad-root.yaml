apiVersion: v1
kind: Pod
metadata:
  name: bad-root
  namespace: demo
spec:
  securityContext:
    runAsUser: 0
  containers:
  - name: nginx
    image: nginx:1.27
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
